name: lint-every-pr
on:
  pull_request:
    branches:
      - master
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: marian-code/python-lint-annotate@v3
        with:
          python-version: "3.9"
      